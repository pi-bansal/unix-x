[package]
name = "aiux"
version = "0.1.0"
edition = "2021"
description = "Dispatcher — run any aiutilx tool via `aiux <tool> [args...]`"

[[bin]]
name = "aiux"
path = "src/main.rs"

[dependencies]
serde_json = "1"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
